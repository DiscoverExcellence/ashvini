%h3 Matches
%table.table
  %tr
    %th
      No. of players
    %th
      Venue
    %th
      Played on
     
    %th{:colspan => "3"}
      Actions
  - @matches.each do |t|
    %tr
      %td= t.no_of_player
      %td= t.venue
      %td= t.played_on
      %td=link_to "Show", match_path(t)
      %td=link_to "Edit", edit_match_path(t)
      %td=link_to "Delete", match_path(t), method: :delete, confirm: "Are you sure?"
