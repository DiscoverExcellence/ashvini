%table{:border => "1"}
  %tr
    %th
      Name
    %th
      Number of matches
    %th
      winner
    %th{:colspan => "3"}
      Actions
  - @tournaments.each do |t|
    %tr
      %td= t.name
      %td= t.matches.count
      %td= t.winner
      %td=link_to "Show",tournament_path(t)
      %td=link_to "Edit",edit_tournament_path(t)
      %td=link_to "Delete",tournament_path(t), method: :delete
